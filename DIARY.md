# 碎碎念日记 · 及时反馈 (MVP)

一个最小可行的记日记工具：你用**自己习惯的编辑器**在 `diary/` 文件夹里写 `.md`，
一停笔，脚本就在你这段话下面自动追加一段 AI 的 **🪞 洞察** —— 不灌鸡汤、不堆建议，
而是像镜子一样帮你看见这段话背后的情绪、模式和要点（还能结合前面的记录看出主题变化）。

有来有回，才记得下去。

## 30 秒上手

1. 准备一个 Anthropic API Key，在项目根目录建一个 `.env`：

   ```
   ANTHROPIC_API_KEY=sk-ant-...
   ```

2. 启动监听（零依赖，只要 Node 18+）：

   ```bash
   node scripts/diary.mjs
   ```

3. 用任何编辑器打开 `diary/` 里的 `.md`（VS Code、Typora、Obsidian、vim……都行），
   写下你的碎碎念。**写完停笔几秒**，反馈会自动出现在下面。

## 长这样

你写：

```
今天又一整天没推进项目，刷手机刷到半夜，明明知道不该这样。
```

几秒后文件里自动多出：

```
<!-- ai -->
> 🪞 **洞察** · 23:14
>
> 你说「明明知道不该这样」——卡住你的好像不是不知道，而是开不了头。
> 拖延常常是给「怕做不好」找的缓冲。
<!-- /ai -->
```

`<!-- ai -->` / `<!-- /ai -->` 是给脚本认的标记，渲染时看不见。
脚本只对**最后一个反馈块之后的新内容**做反馈，所以你可以一段段往下写，像聊天一样。

## 几个细节

- **一天一个文件还是一个长文件都行**，脚本监听整个 `diary/` 文件夹里所有 `.md`。
- **不会打扰你打字**：只有内容稳定几秒（默认 3.5s）才会反馈；如果你又开始敲，它会等。
- **启动时不会翻旧账**：只对脚本运行后新写的内容反馈。
- **数据全在本地**，就是这些 Markdown 文件，你的，随便拿。

## 可调项（都在 `.env` 或环境变量里）

| 变量 | 默认 | 说明 |
| --- | --- | --- |
| `ANTHROPIC_API_KEY` | 必填 | 你的 API Key |
| `DIARY_DIR` | `diary` | 监听的文件夹 |
| `DIARY_MODEL` | `claude-sonnet-4-6` | 想要更深的洞察可换成 `claude-opus-4-8` |
| `DIARY_POLL_MS` | `1500` | 多久检查一次文件 |
| `DIARY_STABLE_MS` | `3500` | 停笔多久算「写完」 |
